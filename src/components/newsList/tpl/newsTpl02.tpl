<div class="news imgnum_02" data-page="{{page}}" data-index="{{index}}">
    <section class="titlebar border">
        <span class="title">
            {{title}}
        </span>
    </section>
    <section class="img">
        <img src="{{thumbnail_pic_s}}"/>
        <img src="{{thumbnail_pic_s02}}"/>
    </section>
    <footer>
        <span>{{category}}</span>
        <span>{{date}}</span>
    </footer>
</div>